{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/build/${fileBasenameNoExtension}.app",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}